# Copy to dev/.env to override the default test album mount.
# ALBUMS_PATH is the host path mounted at /albums inside the dev containers.
# Defaults to ../test-album when unset.
ALBUMS_PATH=/path/to/your/photos
