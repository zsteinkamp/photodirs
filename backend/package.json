{
  "name": "photodirs",
  "version": "1.0.0",
  "description": "Public photo and album service with a directory-first approach, RAW support, and a minimal example UI.",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "lint": "npx eslint src/",
    "start:api": "node src/api.js",
    "start:watcher": "node src/watcher.js",
    "start:admin": "node src/admin.js",
    "test": "mocha",
    "watch:api": "nodemon --watch src --watch .eslintrc.js --watch package.json --exec 'clear && npm run lint && npm run test && npm run start:api'",
    "watch:watcher": "nodemon --watch src --watch .eslintrc.js --watch package.json --exec 'clear && npm run lint && npm run test && npm run start:watcher'",
    "watch:admin": "nodemon --watch src --watch .eslintrc.js --watch package.json --exec 'clear && npm run lint && npm run test && npm run start:admin'"
  },
  "mocha": {
    "spec": "src/**/*.spec.js"
  },
  "author": "Zack Steinkamp",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "dayjs": "^1.11.7",
    "eslint-config-prettier": "^9.1.0",
    "express": "^4.19.2",
    "express-winston": "^4.2.0",
    "fastq": "^1.15.0",
    "glob": "^8.1.0",
    "js-yaml": "^4.1.0",
    "mocha": "^9.2.2",
    "node-exiftool": "^2.3.0",
    "nodemon": "^3.0.3",
    "sharp": "^0.32.6"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@typescript-eslint/eslint-plugin": "^7.0.1",
    "@typescript-eslint/parser": "^7.0.1",
    "eslint": "^8.56.0",
    "eslint-config-standard": "^17.1.0",
    "eslint-plugin-import": "^2.29.1",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-prettier": "^5.1.3",
    "eslint-plugin-promise": "^6.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
