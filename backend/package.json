{
  "name": "photodirs",
  "version": "1.0.0",
  "description": "Public photo and album service with a directory-first approach, RAW support, and a minimal example UI.",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "lint": "npx eslint src/",
    "typecheck": "npx tsc --noEmit",
    "start:api": "node --loader ts-node/esm src/api.ts",
    "start:watcher": "node --loader ts-node/esm src/watcher.ts",
    "start:admin": "node --loader ts-node/esm src/admin.ts",
    "test": "mocha",
    "watch:api": "nodemon --watch src -e ts --watch .eslintrc.cjs --watch package.json --exec 'clear && npm run lint && npm run test && npm run start:api'",
    "watch:watcher": "nodemon --watch src -e ts --watch .eslintrc.cjs --watch package.json --exec 'clear && npm run lint && npm run test && npm run start:watcher'",
    "watch:admin": "nodemon --watch src -e ts --watch .eslintrc.cjs --watch package.json --exec 'clear && npm run lint && npm run test && npm run start:admin'"
  },
  "mocha": {
    "spec": "src/**/*.spec.ts",
    "loader": "ts-node/esm"
  },
  "author": "Zack Steinkamp",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "dayjs": "^1.11.7",
    "eslint-config-prettier": "^9.1.0",
    "express": "^4.19.2",
    "express-winston": "^4.2.0",
    "fastq": "^1.15.0",
    "glob": "^8.1.0",
    "js-yaml": "^4.1.0",
    "mocha": "^9.2.2",
    "node-exiftool": "^2.3.0",
    "nodemon": "^3.0.3",
    "sharp": "^0.32.6"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/glob": "^8.1.0",
    "@types/js-yaml": "^4.0.9",
    "@types/mocha": "^10.0.10",
    "@typescript-eslint/eslint-plugin": "^7.0.1",
    "@typescript-eslint/parser": "^7.0.1",
    "eslint": "^8.56.0",
    "eslint-config-standard": "^17.1.0",
    "eslint-plugin-import": "^2.29.1",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-prettier": "^5.1.3",
    "eslint-plugin-promise": "^6.1.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  }
}
